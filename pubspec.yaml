name: dictionary
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.0.0
  get: ^4.6.5
  gherkin: ^3.1.0
  audioplayers: ^6.0.0


  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/words_dictionary.json
    - assets/words_dictionary2.json
